# 对packages\skills进行构建

## Status

COMPLETED

<!--
可选状态：
- TODO
- ACTIVE
- PLAN
- BLOCKED
- COMPLETED
-->

## Priority

P1

<!--
可选优先级：
- P0：紧急 / 阻塞其他工作
- P1：重要
- P2：普通
- P3：低优先级
-->

## Dependencies

- None

<!--
填写依赖的 TASK：
- TASK-XXX
- TASK-XXX
-->

## Decision Needed

- None

<!--
仅当 Status 为 PLAN、文件位于 .agents/ap-config/work-data/tasks/plan/ 时填写。
列出人必须拍板的问题，每条应能用一句话回答。
澄清完成后清空本段。
-->

## Context

<!--
说明为什么需要这个任务。
描述当前系统状态、问题或背景。
不要描述具体实现步骤。
-->

## Goal

build构建生成产物

<!--
明确这个任务最终要解决什么问题。
应该描述结果，而不是实现过程。
-->

## Scope

### In Scope

## packages\skills

-
-

### Out of Scope

-
-
-

<!--
明确允许做什么，以及明确不应该顺手做什么。
-->

## Requirements

- 参考ap进行构建
-
-

<!--
描述必须满足的功能或行为要求。
-->

## Constraints

-
-
-

<!--
描述技术、架构、兼容性、安全性等限制。
例如：
- 不引入新的依赖
- 不修改现有 API
- 遵循 ADR-XXX
-->

## Acceptance Criteria

- [x] `packages/skills` 可通过 `pnpm build` 生成 `dist/` 产物
- [x] 构建配置参考 `ap`（`tsc -p tsconfig.build.json` + NodeNext）
- [x] 现有测试与 typecheck 通过

<!--
什么条件全部满足后，任务才能算完成。
验收标准应该尽可能客观、可验证。
-->

## Related Decisions

- None

## Completion

### Implementation

- 新增 `packages/skills/tsconfig.build.json`，沿用 `ap` 的 NodeNext + `dist` 输出模式
- 在 `packages/skills/package.json` 添加 `build` 脚本、`main` 与 `files` 字段

### Tests

- `pnpm --filter @openworker/skills test`：15 项全部通过

### Validation

- `pnpm --filter @openworker/skills build`：成功生成 `dist/*.js` 与 source map
- `pnpm --filter @openworker/skills typecheck`：通过
- `pnpm lint:fix`：通过

### Notes

- monorepo 内 `exports` 仍指向 `src/index.ts`，不影响 workspace 开发；`main` 指向 `dist` 供发布使用
