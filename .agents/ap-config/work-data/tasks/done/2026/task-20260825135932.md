# 关于弹窗构建时间按用户时区显示

## Task Status(Required)

COMPLETED

## Task Priority(Required)

P1

## Task Dependencies(Required)

- None

## Context(Required)

- None

## Requirements(Required)

- [关于]弹窗上面展示的构建时间应当显示为用户当前时区的时间；
  - 构建时间是构建的时候定死的，然后显示的时候根据当前时区来显示。
  - 展示时间使用dayjs库，没有的话就引入，dayjs库的版本需要统一（使用哪个pnpm-workspace来声明）
  - 展示的时间格式是YYYY-MM-DD HH:mm

## Constraints(Required)

- None

## Agent Notes

构建时刻仍用打包时注入的 ISO；展示改为 dayjs 按本地时区格式化。未改 ADR。

## Completion

### Implementation

- 将 `formatBuildIsoUtcHuman` 重命名为 `formatBuildIsoLocalHuman`，用 dayjs 按本地时区输出 `YYYY-MM-DD HH:mm`
- `AboutOpenworkerModal` 与 `formatAboutAppCopyText` 改用该格式
- 在 `pnpm-workspace.yaml` catalog 声明 `dayjs: ^1.11.13`，desktop 改为 `catalog:`

### Validation

- `pnpm lint:fix`：通过
- `pnpm --filter @openworker/desktop typecheck`：通过

### Notes

- 未在实际 Electron 关于弹窗中点选验证（无桌面运行时）
