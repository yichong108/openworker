# 关于弹窗构建时间按用户时区显示

## Task Status(Required)

COMPLETED

## Task Priority(Required)

P1

## Task Dependencies(Required)

- None

## Context(Required)

- None

## Requirements(Required)

- [关于]弹窗上面展示的构建时间应当显示为用户当前时区的时间；构建时间是构建的时候定死的，然后显示的时候根据当前时区来显示。

## Constraints(Required)

- None

## Agent Notes

## Completion

### Implementation

- 将 `formatBuildIsoUtcHuman` 重命名为 `formatBuildIsoLocalHuman`，使用 `Date` 本地字段与 `Intl.DateTimeFormat` 时区缩写格式化构建时间
- `AboutOpenworkerModal` 与 `formatAboutAppCopyText` 改用本地时区显示

### Validation

- `pnpm lint:fix`：通过
- `pnpm --filter @openworker/desktop typecheck`：通过
