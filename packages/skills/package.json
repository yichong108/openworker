{
  "name": "@openworker/skills",
  "version": "1.0.0",
  "description": "OpenWorker agent skills — SKILL.md 内容包与加载器",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./logger": "./src/logger.ts"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yichong108/openworker.git",
    "directory": "packages/skills"
  },
  "bugs": {
    "url": "https://github.com/yichong108/openworker/issues"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "package.json"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "publint": "pnpm run build && publint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check-npm-auth": "npm whoami --registry=https://registry.npmjs.org/",
    "release": "pnpm run test && pnpm run check-npm-auth && tsc -p tsconfig.build.json && npm publish"
  },
  "dependencies": {
    "ai": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "publint": "catalog:",
    "typescript": "catalog:",
    "vitest": "^4.1.10"
  }
}
