# OpenWorker Desktop 环境变量配置示例
# 复制此文件为 .env.local 并填入实际值
# .env.local 不会被提交到 git

# ===============================
# API 服务（Settings 等）
# ===============================
# 桌面端主进程读写 AppSettings 的后端地址（默认本地 API）
OPENWORKERER_API_BASE_URL="http://127.0.0.1:3100"

# 渲染进程直连 API（Vite 需 VITE_ 前缀；与上方地址保持一致）
# request.ts 优先读 VITE_API_URL，其次 VITE_OPENWORKERER_API_BASE_URL
VITE_API_URL="http://127.0.0.1:3100"
VITE_OPENWORKERER_API_BASE_URL="http://127.0.0.1:3100"

# ===============================
# Native 本地服务（主进程自动拉起）
# ===============================
# 默认 http://127.0.0.1:3200；已有健康实例时复用，不重复 spawn
# OPENWORKER_NATIVE_PORT=3200
# OPENWORKER_NATIVE_BASE_URL="http://127.0.0.1:3200"
# 开发态强制用 Electron 内嵌 Node（ELECTRON_RUN_AS_NODE）冒烟打包路径：
# OPENWORKER_NATIVE_USE_ELECTRON_NODE=1

# ===============================
# Langfuse 追踪配置（可选）
# ===============================
# 用于 LLM 调用追踪和调试，不配置则禁用追踪
# 参考: https://langfuse.com/docs

# Langfuse 服务地址
LANGFUSE_BASE_URL="http://127.0.0.1:3000"

# Langfuse 项目公钥（用于客户端上传追踪数据）
LANGFUSE_PUBLIC_KEY="pk-lf-xxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# Langfuse 项目私钥（用于服务端验证）
LANGFUSE_SECRET_KEY="sk-lf-xxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# 开启 Langfuse 调试日志（开发时可选）
# LANGFUSE_DEBUG=true
