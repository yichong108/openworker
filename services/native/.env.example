# Native 本地覆盖（可选，不提交）
#
# 渠道与端口约定见项目根目录 `.env-dev` / `.env-test` / `.env-prod`。
# `pnpm native:dev` 默认加载 `.env-dev`。
#
# 可选：显式覆盖由 APP_CHANNEL 推导的数据根
# OPENWORKER_HOME=
#
# SQLite database file path.
# Default (when unset): {OPENWORKER_HOME}/native/native.sqlite
# SQLITE_PATH=./data/native.sqlite
