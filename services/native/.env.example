# Native 本地覆盖（可选，不提交）
#
# 渠道与端口约定见项目根目录 `.env-dev` / `.env-test` / `.env-prod`。
# `pnpm native:dev` 默认加载 `.env-dev`。
#
# 可选：显式覆盖由 APP_CHANNEL 推导的数据根
# OPENWORKER_HOME=
#
# Prisma SQLite database URL
# Default (when unset): file:{OPENWORKER_HOME}/native/native.sqlite
# DATABASE_URL="file:./data/native.sqlite"
