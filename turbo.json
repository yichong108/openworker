{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env-dev", ".env-test", ".env-prod", "**/.env.*local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["out/**", "dist/**", ".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "cache": false
    }
  }
}
