# Cursor SDK（本地 Agent）
# 复制为 .env：cp aip/.env.example aip/.env
# 或在仓库根目录 / shell 中导出 CURSOR_API_KEY
#
# 用户 Key：https://cursor.com/dashboard/api
# 也可运行：pnpm aip login

CURSOR_API_KEY=

# 可选；默认 composer-2.5
# CURSOR_MODEL=composer-2.5

# 可选；Cursor SDK 对话模式，默认 agent（直接改代码），plan 为先出方案
# AIP_MODE=agent
